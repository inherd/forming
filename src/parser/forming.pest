alpha = { 'a'..'z' | 'A'..'Z' }
digit = { '0'..'9' }

start = _{ SOI ~ declaration* ~ EOI}

declaration = {
    architecture_declaration
}

architecture_declaration = {
    "architecture"
}

component_declaration = {
    "component"
}
